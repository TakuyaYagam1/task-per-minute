// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.0
// source: leaderboard.sql

package sqlc

import (
	"context"

	"github.com/google/uuid"
)

const totalSolveTimeForPlayers = `-- name: TotalSolveTimeForPlayers :many
SELECT p.id AS player_id,
    p.username AS username,
    COALESCE(
        SUM(
            (
                EXTRACT(
                    EPOCH
                    FROM dpt.solved_at - d.started_at
                ) * 1000
            )::BIGINT
        ),
        0
    )::BIGINT AS total_solve_time_ms
FROM players p
    LEFT JOIN duels d ON d.winner_id = p.id
    AND d.status = 'finished'
    LEFT JOIN duel_player_tasks dpt ON dpt.duel_id = d.id
    AND dpt.player_id = p.id
    AND dpt.solved = TRUE
WHERE p.username = ANY($1::text [])
GROUP BY p.id,
    p.username
ORDER BY total_solve_time_ms ASC,
    p.username ASC
`

type TotalSolveTimeForPlayersRow struct {
	PlayerID         uuid.UUID
	Username         string
	TotalSolveTimeMs int64
}

// Batch variant used by the leaderboard usecase for Redis usernames.
func (q *Queries) TotalSolveTimeForPlayers(ctx context.Context, dollar_1 []string) ([]TotalSolveTimeForPlayersRow, error) {
	rows, err := q.db.Query(ctx, totalSolveTimeForPlayers, dollar_1)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []TotalSolveTimeForPlayersRow{}
	for rows.Next() {
		var i TotalSolveTimeForPlayersRow
		if err := rows.Scan(&i.PlayerID, &i.Username, &i.TotalSolveTimeMs); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const totalSolveTimePerPlayer = `-- name: TotalSolveTimePerPlayer :many
SELECT p.id AS player_id,
    p.username AS username,
    COALESCE(
        SUM(
            (
                EXTRACT(
                    EPOCH
                    FROM dpt.solved_at - d.started_at
                ) * 1000
            )::BIGINT
        ),
        0
    )::BIGINT AS total_solve_time_ms
FROM players p
    JOIN duels d ON d.winner_id = p.id
    AND d.status = 'finished'
    JOIN duel_player_tasks dpt ON dpt.duel_id = d.id
    AND dpt.player_id = p.id
    AND dpt.solved = TRUE
GROUP BY p.id,
    p.username
ORDER BY total_solve_time_ms ASC,
    p.username ASC
`

type TotalSolveTimePerPlayerRow struct {
	PlayerID         uuid.UUID
	Username         string
	TotalSolveTimeMs int64
}

// Aggregates total solve time (ms) across all duels each player has won.
// Ordered ASC: faster total time -> higher rank. Used as a Redis-leaderboard
// tiebreaker when two players share the same win count.
func (q *Queries) TotalSolveTimePerPlayer(ctx context.Context) ([]TotalSolveTimePerPlayerRow, error) {
	rows, err := q.db.Query(ctx, totalSolveTimePerPlayer)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []TotalSolveTimePerPlayerRow{}
	for rows.Next() {
		var i TotalSolveTimePerPlayerRow
		if err := rows.Scan(&i.PlayerID, &i.Username, &i.TotalSolveTimeMs); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
