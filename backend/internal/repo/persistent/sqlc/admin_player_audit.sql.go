// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: admin_player_audit.sql

package sqlc

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createAdminPlayerAuditEvent = `-- name: CreateAdminPlayerAuditEvent :exec
INSERT INTO admin_player_audit_events (
    actor_subject,
    actor_jti,
    action,
    player_id,
    before_state,
    after_state,
    created_at
)
VALUES ($1, $2, $3, $4, $5, $6, $7)
`

type CreateAdminPlayerAuditEventParams struct {
	ActorSubject string
	ActorJti     string
	Action       string
	PlayerID     uuid.UUID
	BeforeState  []byte
	AfterState   []byte
	CreatedAt    pgtype.Timestamptz
}

func (q *Queries) CreateAdminPlayerAuditEvent(ctx context.Context, arg CreateAdminPlayerAuditEventParams) error {
	_, err := q.db.Exec(ctx, createAdminPlayerAuditEvent,
		arg.ActorSubject,
		arg.ActorJti,
		arg.Action,
		arg.PlayerID,
		arg.BeforeState,
		arg.AfterState,
		arg.CreatedAt,
	)
	return err
}

const listAdminPlayerAuditEventsByPlayer = `-- name: ListAdminPlayerAuditEventsByPlayer :many
SELECT id,
    actor_subject,
    actor_jti,
    action,
    player_id,
    before_state,
    after_state,
    created_at
FROM admin_player_audit_events
WHERE player_id = $1
ORDER BY created_at DESC,
    id DESC
LIMIT $2
`

type ListAdminPlayerAuditEventsByPlayerParams struct {
	PlayerID uuid.UUID
	Limit    int32
}

func (q *Queries) ListAdminPlayerAuditEventsByPlayer(ctx context.Context, arg ListAdminPlayerAuditEventsByPlayerParams) ([]AdminPlayerAuditEvent, error) {
	rows, err := q.db.Query(ctx, listAdminPlayerAuditEventsByPlayer, arg.PlayerID, arg.Limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []AdminPlayerAuditEvent{}
	for rows.Next() {
		var i AdminPlayerAuditEvent
		if err := rows.Scan(
			&i.ID,
			&i.ActorSubject,
			&i.ActorJti,
			&i.Action,
			&i.PlayerID,
			&i.BeforeState,
			&i.AfterState,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
